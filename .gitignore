# Sensitive / local-only data — never commit these
data/users.json
data/jobs/*.json
data/lists/*.json
data/smtp/*.json

# Keep the folder structure tracked
!data/jobs/.gitkeep
!data/lists/.gitkeep
!data/smtp/.gitkeep

# OS
.DS_Store
